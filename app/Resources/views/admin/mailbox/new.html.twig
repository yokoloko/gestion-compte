{% extends 'layout.html.twig' %}

{% block breadcrumps %}
    <a href="{{ path('homepage') }}"><i class="material-icons">home</i></a> <i class="material-icons">chevron_right</i>
    <a href="{{ path('admin') }}"><i class="material-icons">tune</i>admin</a> <i class="material-icons">chevron_right</i>
    <a href="{{ path('mailbox_list') }}"><i class="material-icons">list</i>boîtes mail</a> <i class="material-icons">chevron_right</i>
    <i class="material-icons">add</i>nouvelle boîte mail
{% endblock %}

{% block content %}
    <h4 class="">Nouvelle boîte mail</h4>

    {{ form_start(form) }}
    <div class="row">
        <div class="col s6">
            {{ form_row(form.fromName) }}
        </div>
        <div class="col s6">
            {{ form_row(form.address) }}
        </div>
    </div>
    <div>
        <button type="submit" class="btn-large waves-effect waves-light">Créer</button>
    </div>
    {{ form_end(form) }}

{% endblock %}
